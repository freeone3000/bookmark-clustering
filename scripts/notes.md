ab_test.py is AI-generated, so it's got some *problems* that were not worth solving.
Notably, some of the blinded variables are sent in the javascript to the client
instead of doing it server-side.
I won't look at them, but we'll have to fix this later.

# TODO Fix AI screwup. Again.